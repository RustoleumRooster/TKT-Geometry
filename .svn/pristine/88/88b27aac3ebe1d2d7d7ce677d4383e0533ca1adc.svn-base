
uniform sampler2D myTexture;
//uniform sampler2D myTexture2;

//uniform vec3 mLightPos;
varying vec3 v;
//varying float light;

uniform float mLightPos[5];


void main (void)
{
    	vec4 col = texture2D(myTexture, vec2(gl_TexCoord[0]));
    	//vec2 texCoords = v.xy /v.w * 0.5 + vec2(0.5,0.5);

    	vec4 col2 = vec4(1,1,1,1);
	float light=0;

	vec3 z;
	//z.x = mLightPos[0];
	//z.y = mLightPos[1];
	//z.z = mLightPos[2];

	//for(int i=0; i<2; i++)
	//{
    		vec3 r = (v- z);
    		float d = length(r);

    		if(d>200.0) 
        		light += 0;
    		else
        		light += 1.0 - d/200;
	//}

	clamp(light,0,1);

    	//gl_FragColor = vec4(1,1,1,1) * light;
	gl_FragColor = vec4(0,mLightPos[0],0,0);

    	//col2*light;//vec4(0,0.5,0,0); //col*light + col2*(1-light);
}
